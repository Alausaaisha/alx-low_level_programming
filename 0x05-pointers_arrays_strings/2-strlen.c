#include "main.h"
/**
 * int _strlen -  returns the length of a string
 * @*s: string who length is to be printed
 * Return: void
 */
int _strlen(char *s)
{
	char *s;
	int len;

	len = strlen(s);
	
	{
	_putchar("strlen(s) :  %d\n", len);
	}
}
