#include "main.h"
#include <string.h>
/**
 * _strlen - prints the length of a string
 * @s: string length is to be printed
 * Return: string length
 */
int _strlen(char *s)
{
	int len;
	
	len = 0;

	while (*s != '\0')
	{
		l++;
		s++;
	}
	return (l);
}
